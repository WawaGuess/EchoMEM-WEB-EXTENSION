{
  "name": "echomem-web-extension",
  "version": "1.0.0",
  "type": "module",
  "private": true,
  "description": "EchoMem Web Extension",
  "scripts": {
    "build:content": "esbuild src/entry/content.js --bundle --format=iife --target=chrome88 --outfile=dist/content.js",
    "build": "npm run build:content",
    "check": "npm run build && node --check dist/content.js && node --check background.js && node scripts/validate-extension.js",
    "test": "node --test tests/*.test.js",
    "package": "npm run build && ./scripts/package-release.sh && node scripts/validate-extension.js release/EchoMem-Extension"
  },
  "devDependencies": {
    "esbuild": "^0.25.0"
  },
  "dependencies": {
    "three": "^0.160.0"
  }
}
