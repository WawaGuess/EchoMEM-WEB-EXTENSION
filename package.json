{
  "name": "echomem-web-extension",
  "version": "1.0.0",
  "type": "module",
  "private": true,
  "description": "EchoMem Web Extension",
  "scripts": {
    "build:content": "node scripts/build-extension.mjs --profile development --entry content --outdir dist",
    "build:feedback-episode": "node scripts/build-extension.mjs --profile development --entry feedback-episode --outdir dist",
    "build:feedback-summary": "node scripts/build-extension.mjs --profile development --entry feedback-summary --outdir dist",
    "build": "node scripts/build-extension.mjs --profile development --outdir dist",
    "check:generated": "node scripts/check-generated-bundles.mjs",
    "test:platform-detection": "node scripts/test-platform-location.mjs",
    "check": "npm run test:platform-detection && npm run check:generated && npm run build && node --check dist/content.js && node --check dist/feedback-episode.js && node --check dist/feedback-summary.js && node --check background.js && node scripts/validate-extension.js",
    "test": "node --test tests/*.test.js",
    "package:public": "node scripts/package-release.mjs public && node scripts/validate-extension.js release/EchoMem-Extension-Public public",
    "package:intranet": "node scripts/package-release.mjs intranet && node scripts/validate-extension.js release/EchoMem-Extension-Intranet intranet",
    "package": "npm run package:public && npm run package:intranet"
  },
  "devDependencies": {
    "esbuild": "^0.25.0"
  },
  "dependencies": {
    "three": "^0.160.0"
  }
}
